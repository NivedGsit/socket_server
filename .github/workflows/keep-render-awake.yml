name: Keep Render Server Awake

on:
  schedule:
    - cron: "* * * * *"   # every minute
  workflow_dispatch:        # allows manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Server
        run: |
          curl https://socket-server-1-sdhw.onrender.com/health || true
